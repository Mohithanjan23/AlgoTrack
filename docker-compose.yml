version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    depends_on:
      - sandbox

  sandbox:
    build: ./backend/app/sandbox
    image: algotrack-sandbox
